[project]
name = "grret"
authors = []
description = ""
telemetry = false
requirements = []
cache_dir = "/home/runner/workspace/grret/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.obscura-locksmith-protocol]
path = "contracts/obscura-locksmith-protocol.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
